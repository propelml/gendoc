{
  "name": "gendoc",
  "version": "0.0.1",
  "main": "dist/gendoc.js",
  "repository": "git@github.com:propelml/gendoc.git",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc",
    "fmt": "prettier --trailing-comma all --write *.ts",
    "lint": "tslint --project tsconfig.json",
    "test": "ts-node gendoc_test.ts"
  },
  "devDependencies": {
    "@types/node": "^10.0.2",
    "liltest": "^0.0.5"
  },
  "dependencies": {
    "typescript": "^2.8.3"
  }
}
